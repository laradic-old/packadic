var getSlug=require("../lib/speakingurl");describe("getSlug defaults",function(){it("should replace whitespaces with separator",function(e){getSlug("foo bar baz").should.eql("foo-bar-baz"),e()}),it("should remove trailing space if any",function(e){getSlug(" foo bar baz ").should.eql("foo-bar-baz"),e()}),it("should remove multiple whitespaces",function(e){getSlug(" foo bar  baz   FOO    BAR      BAZ      ").should.eql("foo-bar-baz-foo-bar-baz"),e()}),it("should remove multiple separators at start and end",function(e){getSlug("-foo- bar -baz-").should.eql("foo-bar-baz"),getSlug("--foo- bar -baz---").should.eql("foo-bar-baz"),getSlug("---foo- bar -baz---").should.eql("foo-bar-baz"),e()}),it("should remove multple separators",function(e){getSlug("foo- bar -baz").should.eql("foo-bar-baz"),e()}),it("should remove non-base64 characters",function(e){var t=["[","]",",","*","+","~",".","(",")","'",'"',"!",":","@"];for(var n=0;n<t.length;n++)getSlug("foo "+t[n]+" bar baz").should.eql("foo-bar-baz");e()}),it("should remove trailing separator",function(e){getSlug("C'est un beau titre qui ne laisse rien à désirer  ! ").should.eql("c-est-un-beau-titre-qui-ne-laisse-rien-a-desirer"),e()}),it("should handle whitespace after symbol",function(e){getSlug("∆299").should.eql("delta-299"),getSlug("∆world").should.eql("delta-world"),getSlug("∆-299").should.eql("delta-299"),getSlug("∆-world").should.eql("delta-world"),getSlug("(∆)299").should.eql("delta-299"),getSlug("(∆)299",{mark:!0}).should.eql("(delta)299"),getSlug("∆299").should.eql("delta-299"),getSlug("∆world").should.eql("delta-world"),getSlug("Hello∆299").should.eql("hello-delta-299"),getSlug("299∆Hello").should.eql("299-delta-hello"),e()}),it("should not fail if symbol at the end",function(e){getSlug("test &").should.eql("test-and"),getSlug("test & ").should.eql("test-and"),getSlug("test &","_").should.eql("test_and"),getSlug("test ♥").should.eql("test-love"),getSlug("test ♥ ").should.eql("test-love"),getSlug("test ♥  ").should.eql("test-love"),e()})});